import colors  from './colors'

const fonts = {
  title: {
    fontFamily: 'System',
    fontSize: 40,
    color: colors.dark
  },
  body: {
    fontFamily: 'System',
    fontSize: 14,
    color: colors.dark,
  }
}

export default fonts
